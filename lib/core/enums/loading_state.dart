enum LoadingState { loading, data, error }

enum LoadingWithdrawalState { loading, data, error }

enum LoadingMetalHistoryState { loading, data, error }

enum LoadingMoneyHistoryState { loading, data, error }

enum LoadingLoanState { initial, loading, data, error, empty }
